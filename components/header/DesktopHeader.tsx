export default function DesktopHeader({ className }: { className: string }) {
  return (
    <header className={`${className}`}>
      <nav>
        <ul>
          <li></li>
        </ul>
      </nav>
    </header>
  );
}
